version: "3"

services:
  web:
    build:
      context: ./
      dockerfile: ./Dockerfile_App
    container_name: "app"
    ports:
      - "8080:8080"
    restart: "unless-stopped"

  mongodb:
    image: mongo
    container_name: mongodb-commission
    ports:
      - "27017:27017"